name: Deploy to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Kolkata
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive

      - name: Install Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: 'latest'
          extended: true

      - name: Validate taxonomy content
        run: pwsh -File ./scripts/validate-taxonomy.ps1

      - name: Validate content quality
        run: pwsh -File ./scripts/validate-content-quality.ps1

      - name: Build
        run: hugo --minify --cleanDestinationDir --baseURL "https://ashish-sites.github.io/sensemaker-site/"

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          force_orphan: true
          keep_files: false
